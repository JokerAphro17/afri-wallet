import React from 'react';
import PageHeader from '../../../../layouts/components/page-header';

const NewspapersSideClient = (props) => {
    return (
        <>
        <PageHeader title={'Journaux'} />
            NewspapersSideClient
        </>
    );
}

export default NewspapersSideClient;
