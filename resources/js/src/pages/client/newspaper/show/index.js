import React from 'react';

const ShowNewspaperSideClient = (props) => {
    return (
        <div>
            ShowNewspaperSideClient
        </div>
    );
}

export default ShowNewspaperSideClient;
